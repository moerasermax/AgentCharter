# <PROJECT_NAME> Institutional Memory（制度記憶）

> **用途**：將先前踩過的坑轉化為可傳承的制度資產。看到相似症狀時先翻這份、再動手。
> **維護原則**：每一條都有「症狀 → 根因 → 診斷 → 修法 → 預防」**五段格式**，讓後手能在 30 秒內判斷要不要走這條。
> **版本歷史**：<YYYY-MM-DD> 建立 / 升級紀錄。

---

## L<N> · <章節主題>（來源：<觸發事件 ID>）

### 症狀

<具體症狀描述 — 用清單列出可觀察現象>

- <症狀 1：log / DB 內容 / UI 表現 / 數值偏差>
- <症狀 2：可重現步驟>
- <症狀 3：邊界場景>

### 根因

<具體根因描述。引用：>

- 檔案路徑 + 行號
- 程式碼片段（必要時）
- 違反的領域公理 / 條款編號
- 為何 v0.x 之前未發現

### 診斷

<可重現的診斷步驟。每步含具體指令。>

1. <第一步診斷指令 + 期望輸出>
2. <第二步比對外部來源>
3. <第三步收斂結論>

### 修法

```<語言>
<具體修法程式碼或操作步驟>
```

<說明此修法的取捨、為何選此而非他案>

### 預防

- <預防原則 1：寫入哪個檢查清單 / 哪個工具自動化>
- <預防原則 2：何時該回頭讀此章節（關鍵詞 / 場景）>
- <預防原則 3：跨類比場景 — 此教訓在哪些其他模組也適用>

---

## 模板使用指南

實例化此模板時替換以下變數：

| 變數 | 範例 |
|---|---|
| `<PROJECT_NAME>` | CryptoBot / 你的專案名 |
| `<N>` | L1, L2, L3, ... 或 §S70（依專案命名慣例）|
| `<章節主題>` | 一句話概括（如「BingX 精度解析陷阱」「跨時區資料判讀」）|
| `<觸發事件 ID>` | 對應的 capsule / handoff 編號 |

### 五段格式強制

- 不可省略任何一段
- **症狀**段須含可觀察現象（不能只寫「有 bug」）
- **根因**段須引具體檔案 + 行號或程式碼片段
- **診斷**段須含可重現指令
- **修法**段須含程式碼或具體操作
- **預防**段須含「何時該回頭讀本章節」的觸發條件

省略段落 = 違反 `core/structural-anti-fabrication.md`，抽驗方有權直接退稿。
