# <PROJECT_NAME> 專案交接文件 #<N> (Checkpoint: <milestone-or-event-tag>)

> **版本**：<Milestone X / Event Y>
> **日期**：<YYYY-MM-DD>
> **狀態**：<高層描述本 session 結束時的整體狀態>

---

## 📌 里程碑摘要

<本 session 達成的高層目標 — 1〜3 段。含關鍵事件、重大決策、是否觸發升級協議。禁吞 hotfix 進主敘事。>

---

## 🔥 核心事件與資產沉澱

### 1. <事件名稱>

| 階段 | 內容 |
|---|---|
| <階段 A> | <描述> |
| <階段 B> | <描述> |

### 2. <子事件 / 真因 / 修法>

**檔案**：`<path>`

| <欄> | <欄> | <欄> |
|---|---|---|
| <值> | <值> | <值> |

### 3. src/ 變更檔案清單

| 檔案 | 性質 |
|---|---|
| `<path>` | 🆕 新增 / 改寫 / DTO 變更 / ... |

**領域公理對齊**：<逐項列>

### 4. 測試指標

| 專案 | 通過 / 總計 | 對照前次 HANDOFF 基線 |
|---|---|---|
| <test project> | N / N | <增減描述> |
| **合計** | **N / N** | **<升降>** |

`<build command>` 0 警告 0 錯誤。

---

## 📂 完整膠囊清單（本 session）

| 膠囊 | 狀態 |
|---|---|
| `<TASK_ID>_<DESC>` | ✅ 完成 / ⛔ VOIDED / 🟡 進行中 |

**禁止吞描述**：本 session 軌跡含「<段落 1> → <段落 2> → ...」N 段，每段都有獨立決策點，不應壓縮成一行。下個 session 接班者讀本 HANDOFF 必須能看到完整事件鏈。

---

## 📜 Protocols 版本迭代軌跡

| 文件 | 變更 | 觸發 |
|---|---|---|
| `<protocol doc>` | <版號變化 + 涵蓋條款>| <為何修> |

---

## 🧠 Institutional Memory 新增段落引述

`<§章節編號>`：

- **何時要回頭讀**：<具體場景>
- **核心預防原則**：<關鍵原則摘要>

---

## 📊 技術指標

| 項目 | 值 |
|---|---|
| Build 警告 | <N> |
| Build 錯誤 | <N> |
| 測試通過率 | <N%>（<P/T>）|
| 新增測試 | <N> |
| VCP 覆蓋情境 | <N>（<列出綠的情境編號>）|

---

## ⚠️ 紀律事件總結（如本 session 發生失敗模式累積，下版必讀）

### F 模式累積

| 事件 | F1 | F2 | F3 | F4 | F5 |
|---|---|---|---|---|---|
| <事件名稱>（本 session）| <N> | <N> | <N> | <N> | <N> |

### 結構性教訓

<列出本次累積後得到的結構性教訓，建議寫入 IM 或推進下版條款>

---

## 🚀 下一階段預告（Up Next）

### 後續觀察項

- 🟡 <可選驗收項，本 session 未跑或不阻擋>
- 🟡 <下版需補強>

### NextWork 推進

- [<Tag>] <任務描述> — <當前狀態>

### 制度面待議

- <下版 protocol 改善候選>

---

## 📝 待 commit 清單

接班者下達 commit 指令後將以下檔案打包提交：

```
<list of files>
```

建議 commit 訊息：

```
<type(scope): subject>

<body>

Co-Authored-By: <co-author>
```

---

## 模板使用指南

實例化此模板時替換以下變數：

| 變數 | 範例 |
|---|---|
| `<PROJECT_NAME>` | CryptoBot / 你的專案名 |
| `<N>` | 連續遞增整數，從 `<最新 HANDOFF_N.md> + 1` |
| `<milestone-or-event-tag>` | 短語 tag，便於 grep |
| `<Domain Axiom>` | 引用 domain-axioms.md 條款 |

序號規則：`HANDOFF_<N>.md` 中 N 為連續遞增整數，從 1 開始。可用 `ls -1 handoffs/HANDOFF_*.md | sort -V | tail -1` 取最新。
