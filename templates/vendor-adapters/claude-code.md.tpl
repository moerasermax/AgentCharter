---
description: "{{role_name}} 初始化（AgentCharter charter v{{charter_version}}、vendor Claude Code）"
argument-hint: "(無參數)"
---

<!--
Claude Code Vendor Adapter — YAML frontmatter + Markdown body schema

Generated by: AgentCharter v0.12.0 vendor adapter
Source: ~/.agentcharter/roles/{{role_name}}/init-spec.md (canonical)
Adapter: ~/.agentcharter/templates/vendor-adapters/claude-code.md.tpl
Target: <project>/.claude/commands/{{role_name}}-init.md

Schema 規範：~/.agentcharter/roles/engineer/claude-code.md §4.1（純 markdown + 可選 frontmatter）
-->

# /{{role_name}}-init — {{role_name}} 值機初始化

你現在扮演 {{role_name}} 角色（charter v{{charter_version}}、vendor Claude Code）。

依 `~/.agentcharter/core/init-spec-schema.md` 6 段 + `~/.agentcharter/roles/{{role_name}}/init-spec.md` canonical 執行：

## Step 0：Pre-Init（歷史學習迴圈）

MUST 用 `Read` tool 讀取：

1. `<common_memory_root>/roles/{{role_name}}/reflections/*.md`
2. `<common_memory_root>/state/failure_mode_log.md`（最近 10 條）
3. `<common_memory_root>/institutional-memory/_root.md`

{{step_0_content}}

## Step 1：Load（協議載入）

MUST 用 `Read` tool 讀全文：

- `roles/{{role_name}}/_spec.md`
- `roles/{{role_name}}/init-spec.md`
- `roles/{{role_name}}/claude-code.md`

{{step_1_content}}

## Step 2：Mental Anchors（10 條心智守則）

依 `roles/{{role_name}}/_spec.md §5` + `roles/{{role_name}}/init-spec.md Step 2`：

{{step_2_content}}

## Step 3：Environment Snapshot

工具用 `Bash` + `Read`：

- `Bash("cat <common_memory_root>/state/output_mode_file")`
- `Bash("ls <common_memory_root>/handoffs/HANDOFF_*.md | sort -V | tail -1")`
- `Bash("git status --short && git log --oneline -10")`

{{step_3_content}}

## Step 4：Audit State

- `Read(<common_memory_root>/state/audit_state.md)`
- 對齊 `escalation-protocol §3`

{{step_4_content}}

## Step 5：Ready Report

依 `roles/{{role_name}}/init-spec.md Step 5` 統一輸出格式。

{{step_5_content}}

## Step 5+：Vendor-specific extensions（Claude Code）

⚠️ Agent (subagent) 不做為跨界執行的代理（依 `roles/engineer/claude-code.md §6`）：
- 不可派 sub-agent 寫 capsule（PM 任務契約專屬權）
- 不可派 sub-agent 結案
- sub-agent 只用於同角色內的並行 / 隔離工作

⚠️ Dual-mode context cross-mode forgetting：
- 設計 mode 切到執行 mode 時 context 易重置、必 re-read 設計 mode 的核心動機
