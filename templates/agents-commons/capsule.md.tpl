# 膠囊：<TASK_ID>_<SHORT_DESC>

> **建立日期**：<YYYY-MM-DD>
> **位階**：<說明本膠囊為新建 / 取代 / 補強 / hotfix 等>
> **依據**：<觸發來源，如使用者回報 / 抽驗發現 / 上一張膠囊延伸>

---

## 1. 動機與背景 (Context)

<為何要做這件事 — 1〜3 段，含使用者觀察 / 實證證據 / 違反鐵律的描述>

違反 <Domain Axiom> ：<具體違反條款 + 後果>

## 2. 根因 (Root Cause)

<具體檔案 + 行號 + 程式碼片段。禁止「應該是」「可能」這類模糊描述，須有實證。>

**檔案**：`<relative/path/to/file.ext>`

### Bug #N — <一句話描述>（line <N>）

```<語言>
<原始程式碼片段>
```

<說明為何錯、影響範圍>

### 不在範圍內（已實證無 bug）

| 元件 | 實證結果 |
|---|---|
| <檔案 / 模組> | <為何不動> |

## 3. 修法範圍 (Scope)

**只動以下檔案**：

| 檔案 | 變更性質 |
|---|---|
| `<path>` | 新增 / 改寫 / DTO 變更 / ... |

**禁止動**：`<path>`、`<path>`（已實證正確，動之即引入新 bug）。

## 4. 修法方案 (Strategy)

### 4.1 <方案名 / Bug #N 修法>

```<語言>
<具體修法程式碼或偽碼>
```

<說明設計取捨、為何採此方案>

### 領域公理對齊

- <Axiom A>：<本修法如何對齊>
- <Axiom B>：<本修法如何對齊>

## 5. 驗收計畫 (VCP)

> 依 `core/completion-delivery.md` 結構：含 Directive Header / Pre-flight + 雙保險 / 危險度標籤 / 期望錨點 / 失敗解讀表。

### 5.1 前置檢查（Pre-flight）

📖 唯讀。<領域雙保險，如「Demo 雙保險：本驗收不下任何真單」>。

```
<具體前置檢查指令>
```

**期望錨點**：<關鍵字串 / 狀態碼>

### 5.2 測試情境 N

#### 情境 1：<情境名> 📖 唯讀 / ⚠️ 會建立紀錄 / 🔥 會動到資金

**目的**：<本情境驗證的具體行為>

```
<可一鍵複製的指令，禁加行號>
```

**期望錨點**：<具體字串 / 狀態碼錨點>

**失敗解讀表**：

| 觀察到 | 代表 | 修法 |
|---|---|---|
| <觀察 X> | <代表 Y> | <修法 Z> |

#### 情境 2 ...

### 5.3 整體驗收判定

| 結果組合 | 結論 | 動作 |
|---|---|---|
| 情境 1-N 全綠 | ✅ 可關膠囊 | <執行方標註結案> |
| 情境 X 紅 | <對應風險> | <退稿 / 修法> |

## 6. 權責邊界 (Role)

- <Role A>：<該角色職責>
- <Role B>：<該角色職責>

## 7. 連動更新

本膠囊執行時應一併更新：
- `<institutional-memory entry>` — <章節主題>
- `<protocols>` — <可能升級的條款>

---

## 歷史紀錄區（驗收回寫）

> 依 `core/structural-anti-fabrication.md`：每項驗收結果含 stdout 區塊；空區塊或純文字描述視同未交付。

### <YYYY-MM-DD> — <角色> <輪次>驗收

**情境 N stdout**：

```bash
<指令>
```

```text
<原文 stdout>
```

**判定**：<綠 / 紅 + 引用失敗模式編號>

### <YYYY-MM-DD> — 結案

- <執行方>結案：✅ 可關膠囊
- 鐵律對齊：<逐項列>
- 連動更新：<列引用 IM 章節 / protocols 變更>

---

## 模板使用指南

實例化此模板時替換以下變數：

| 變數 | 範例 |
|---|---|
| `<TASK_ID>` | `TASK_S70` / `TICKET_123` / `INC-2026-001` 等專案約定 |
| `<SHORT_DESC>` | 短描述（snake_case 或 SCREAMING_SNAKE_CASE）|
| `<Domain Axiom>` | 引用 domain-axioms.md 內具體條款編號 |
| `<Role A/B>` | engineer / pm / reviewer 等實際扮演角色 |

依專案實際 domain-axioms 與啟用的角色 spec 補完內容。
