## v0.5.9 Baseline Snapshot

> **建立**：2026-04-28
> **目的**：v0.5.10 + v0.6.0 大工程動工前的基準點，供完工後差異對照 + 升版流程實證素材
> **git tag**：`v0.5.9` @ `a24c15c` / `pre-v0.6.0-batch` @ `2225659`

---

## 1. Git 座標

| 標記 | Commit | 對象 |
|---|---|---|
| `v0.5.9` (annotated) | `a24c15c` | v0.5.9 release commit（回歸純規範框架 + agent-commons 結構穩定承諾）|
| `pre-v0.6.0-batch` (annotated) | `2225659` | 大工程動工 baseline（含 v0.5.9 後累積的 dogfood signal #4 + #5）|
| HEAD | `2225659` | 動工起點 |

**v0.5.9 → 2225659 之間 commit**：

| Commit | 主題 |
|---|---|
| `e0dcc01` | maintainer-selfcheck DRAFT + ai-vendor 邀請制議題 |
| `2a8a03f` | dogfood signal #4 候選 + self-instantiation 驗證條款修訂候選 |
| `d226100` | YC_AIAgentCrew 接入里程碑 + validator/HANDOFF 排序議題 |
| `2225659` | dogfood signal #5 候選 — LLM 找路徑繞過角色約束 |

→ 全為 `chore(claude_temp)` 議題累積，無條款修訂。

---

## 2. 20 條 core 條款清單（v0.5.9 凍結狀態）

按概念分組：

### A. 角色與職權（4 條）

| 檔案 | 一句話 |
|---|---|
| `core/role-separation.md` | 程式碼權與結案權對稱分離 |
| `core/audit-rights.md` | 抽驗權不得放棄；結案宣告默認待抽驗 |
| `core/role-conflict-resolution.md` | 角色決策衝突三級階梯（v0.5.3）|
| `core/multi-role-tracking.md` | 1 AI 多角色審計（v0.5.4）|

### B. 失敗 / 違規 / 升級（4 條）

| 檔案 | 一句話 |
|---|---|
| `core/failure-modes.md` | F1〜F5 |
| `core/structural-anti-fabrication.md` | 缺 stdout 即退稿（v0.2 全模式）|
| `core/violation-reflection.md` | 違規後須補反省 |
| `core/escalation-protocol.md` | ≥2 次升級強化抽驗、≥3 次裁決 |

### C. 證據與交付（3 條）

| 檔案 | 一句話 |
|---|---|
| `core/evidence-first.md` | 隱性 bug 嚴禁盲猜 |
| `core/output-mode-protocol.md` | eco / verbose 雙段式 |
| `core/completion-delivery.md` | VCP 必含 Directive Header |

### D. 交接 / 跨 AI（4 條）

| 檔案 | 一句話 |
|---|---|
| `core/handoff-chain.md` | session 末交接鏈 |
| `core/cross-ai-handoff.md` | 跨 AI 接班（v0.5.2）|
| `core/working-stack-discipline.md` | DRAFT 暫存堆疊（v0.5.7）|
| `core/init-template.md` | Role Init Mandate 四職責（v0.5.0/v0.5.1）|

### E. 架構 / 配置 / 版本（5 條）

| 檔案 | 一句話 |
|---|---|
| `core/common-memory-root.md` | 架構級（v0.4.1）|
| `core/charter-config.md` | profile + mapping schema |
| `core/domain-axiom-slot.md` | 領域 > 通用 優先序（v0.5.5）|
| `core/versioning-migration.md` | SemVer 對 charter 含義（v0.5.6 + v0.5.9 §2.3 結構穩定承諾）|
| `core/maintainer-discipline.md` | maintainer-only（v0.5.8）|

**合計：20 條**（4+4+3+4+5）

### 位階分類

| 類別 | 條款 | 數量 |
|---|---|---|
| 架構級前提（不設 enabled 開關）| `common-memory-root` / `charter-config` | 2 |
| Maintainer-only（三 preset 預設關）| `maintainer-discipline` | 1 |
| 採用方條款（受 enabled 開關控制）| 其餘 17 條 | 17 |

---

## 3. 三 Preset 啟用矩陣

| Preset | charter_version | 採用方 enabled | 預期值 |
|---|---|---|---|
| `tools/profiles/minimal.yaml` | **`"0.5.8"`** ⚠️ | 9 / 17 | 9 |
| `tools/profiles/standard.yaml` | **`"0.5.8"`** ⚠️ | 17 / 17 | 17 |
| `tools/profiles/strict.yaml` | **`"0.5.8"`** ⚠️ | 17 / 17 | 17 |

⚠️ **Spec-sync 漏**：v0.5.9 release 沒把三個 preset 的 `charter_version` 從 `0.5.8` 升到 `0.5.9`。屬輕微違反 maintainer-discipline §2.2 同步紀律。**v0.5.10 順手修**（搭便車到 #1 / #4 修訂裡）。

各 preset enabled 細節：
- minimal：6 條軟性紀律 + 3 條架構級前提類（`role-separation` / `audit-rights` / `init-template` 等）= 9 條
- standard / strict：17 條全啟用（差異在 parameters 嚴格度）
- 三者皆 `maintainer-discipline: false`（採用方無關）

---

## 4. agent-commons/ 結構（v0.5.0 後標準）

```
project-root/
└── agent-commons/                  ← 採用識別（v0.4.1 架構級約定）
    ├── _config/                    ← 配置層（v0.5.0 合併）
    │   ├── profile.yaml
    │   └── mapping.yaml
    ├── capsules/                   ← 任務膠囊
    ├── handoffs/                   ← HANDOFF 鏈
    ├── protocols/                  ← 領域公理 + 紀律
    ├── institutional-memory/       ← 跨事件知識沉澱
    ├── nextwork.md
    ├── state/                      ← 工具狀態檔
    └── roles/                      ← 角色私有區
        ├── engineer/{_role.md, sessions/, drafts/, reflections/, private/}
        └── pm/...
```

**v0.5.9 §2.3 凍結承諾**：v1.0 後永久不破壞既有採用方此結構（v1.x → v1.y 採用方結構零變更）。

---

## 5. Templates（6 份，v0.4.2 完整萃取自 CryptoBot）

| 檔案 | 用途 |
|---|---|
| `templates/agent-commons/capsule.md.tpl` | 任務膠囊 |
| `templates/agent-commons/handoff.md.tpl` | session 交接 |
| `templates/agent-commons/institutional-memory-entry.md.tpl` | 跨事件知識沉澱 |
| `templates/agent-commons/nextwork.md.tpl` | 任務追蹤 |
| `templates/agent-commons/domain-axioms.md.tpl` | 領域公理範本 |
| `templates/agent-commons/_role.md.tpl` | 角色簽名 |
| `templates/role-init.md.tpl` | self-instantiation 範本（**v0.5.10 #4 待修 HANDOFF 排序 wording**）|
| `templates/management-layout.md` | 採用方目錄結構說明 |

---

## 6. Tools Spec（純規範，v0.5.9 起無 python 實作）

| 檔案 | 用途 | v0.5.9 狀態 |
|---|---|---|
| `tools/init-spec.md` | `/charter-init` 五步驟 | spec-driven，無實作 |
| `tools/doctor-spec.md` | `/charter-doctor` 健康檢查 | spec-driven，無實作（**v0.5.10 #1 待加 self-instantiation 結尾驗證點段**）|
| `tools/scan-spec.md` | `/charter-scan` 既有專案判讀 | spec-driven，無實作 |

**v0.5.9 重大轉向**：移除 `tools/charter-init.py` + `tools/charter-doctor.py`（git history 保留）。所有工具動作改由 AI 依 spec 自具象化。

---

## 7. Roles（兩個角色，v0.5.9 凍結）

| 角色 | 概念層 | Vendor 層 |
|---|---|---|
| `roles/engineer/` | `_spec.md` | `claude-code.md` |
| `roles/pm/` | `_spec.md` | `gemini-cli.md`（2026-04-27 完成，A1 公理首份雙 AI 範本）|

**v0.6.0 預計擴張**：
- 新增 `roles/auditor/`（maintainer-only 位階，僅概念層）
- 新增 `roles/validator/`（採用方角色，僅概念層；vendor 層留待邀請 Gemini 寫）
- `roles/pm/_spec.md` 加 deprecation note（驗證職責漸進轉移）

---

## 8. 九個架構級概念（v0.5.9 凍結）

1. Common Memory Root（v0.4.1）
2. AI Self-Instantiation（v0.5.1）
3. Cross-AI Handoff 全鏈（v0.5.2）
4. 失敗 ⊥ 分歧 正交軸（v0.5.3）
5. 角色 ⊥ 載體 防呆（v0.5.4）
6. 領域 > 通用 優先序（v0.5.5）
7. 版本演化雙軌（v0.5.6）
8. 三種接班場景正交完整（v0.5.7）
9. 純規範框架 + agent-commons 結構穩定承諾（v0.5.9）

**v0.6.0 候選新概念**（暫定）：
10. **角色擴展邀請制 / vendor 不代寫原則**（從 init-template §3.3「框架不代生成」延伸到 vendor 接入 + 新角色誕生）
11. **角色身份穩定性 / 繞路禁令**（multi-role-tracking + role-separation 加新段，封閉 LLM completionist 繞路傾向）

---

## 9. 已對外實證（v0.5.9 為止）

- CryptoBot S70（v0.x 全程使用）
- YC_AIAgentCrew 接入完成（2026-04-28）— 第二個非 CryptoBot 採用案例，v0.5.9 純規範框架首次外部驗證；dogfood signal #4 預測完全成立

---

## 10. v0.5.10 + v0.6.0 工程預期差異（完工後對照用）

### v0.5.10 預期變動

| 軸 | 變動 |
|---|---|
| 條款 | 不增不減，仍 20 條 |
| Templates | `role-init.md.tpl` HANDOFF 排序 shell command wording 修訂 |
| Spec | `init-template.md §3.3.2` 加第 7 步 doctor 驗證；`tools/doctor-spec.md` 加 self-instantiation 結尾驗證點段 |
| Failure modes | 評估 F6（schema 違規未當下抓到、轉嫁下個 AI）|
| QUICKSTART | Step 4-5 wording 修訂 |
| Preset | `charter_version` 三檔同步升至 `0.5.10` |
| 新 dogfood | 條款修訂走 `versioning-migration §3` 7 步流程實證 |

### v0.6.0 預期變動

| 軸 | 變動 |
|---|---|
| 條款 | **20 → 21**（新增 `core/ai-vendor-onboarding.md`）|
| 條款修訂 | `role-separation`（繞路禁令段）/ `multi-role-tracking`（身份穩定承諾段、上岸需 user 授權）/ `role-conflict-resolution`（角色切換決策權屬 user）/ `roles/pm/_spec.md`（deprecation note）|
| Roles | **2 → 4**（新增 `auditor` + `validator` 概念層 spec）|
| Vendor spec | `roles/pm/gemini-cli.md` 補 sub-agent 跨界禁令段 |
| Charter-config | enabled 清單加 `validator`（採用方）+ `auditor`（maintainer-only）|
| Preset | 三 preset enabled 重新計算（`validator` 加入採用方計數）|
| 架構級概念 | **9 → 11**（新增「邀請制原則」+「身份穩定 / 繞路禁令」）|
| BREAKING-LITE | PM 抽驗職責漸進 deprecate（v0.x 並存、v1.0 移除）|

---

## 11. 升版測試素材（後續使用）

完成 v0.6.0 後，這 baseline 可作為以下測試起點：

| 測試 | 用途 |
|---|---|
| `versioning-migration §3` 7 步遷移流程實證 | v0.5.9 → v0.5.10（小步暖身）/ v0.5.10 → v0.6.0（大跳測 BREAKING-LITE）|
| YC_AIAgentCrew 升版實證 | 既有採用方第一次升版痛點 |
| `agent-commons/` 結構穩定承諾驗證 | v0.5.9 §2.3 寫的「不破壞既有採用方」是否真的成立 |
| `/maintainer-selfcheck` skill 落地驗證（若同步推進）| 跨檔引用完整性 dogfood 閉環 |

---

## 12. 非範圍（明示記錄）

本工程**不做**以下事項：

- 🟡 LICENSE 決定 / CryptoBot 重組 / IRON Pattern 抽出 / walkthrough 文件
- 🟢 B2 子條款層級配置 / AgentCharter 自身 dogfooding / 跨 AI CLI
- ⚪ user 全域 skill 路徑硬編碼修法 / 多 AI 拓撲分類 / 條款命名統一 / 多語系策略
- ⚪ AI 能力評估盲區（合併進 #5 邀請制原則處理）

→ 全部留給 v0.6.0 之後或 v1.0 階段。
