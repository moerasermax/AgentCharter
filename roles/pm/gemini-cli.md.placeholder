# PM × Gemini CLI — Implementation（待 Gemini 端提交）

> **狀態**：placeholder（v0.1）
> **基於**：`roles/pm/_spec.md`
> **AI**：Google Gemini CLI

---

## 邀請

本檔留空殼，邀請 Gemini CLI（或代理 Gemini 的人類維護者）依 `roles/pm/_spec.md` 規範提交此 AI 的 PM 實作版本。

---

## 必含內容（請依此清單填寫）

### 1. 工具能力清單

| 能力 | 支援？ | 備註 |
|---|---|---|
| Slash command | ? | 語法 / 註冊位置 |
| Hook 系統 | ? | 對應 Claude Code UserPromptSubmit 的等效機制 |
| Shell 執行 | ? | 是否能跑 git / sqlite3 / dotnet |
| File read/write | ? | 受限範圍 |
| 持久 memory | ? | 跨 session 機制 |
| 多模態（圖 / PDF）| ? | |
| Web fetch / search | ? | |
| 跨 session 持久狀態 | ? | |

### 2. Spec §3 各職責的執行細節

依 `roles/pm/_spec.md §3` 各項職責，描述 Gemini CLI 端的具體操作方式。

特別注意：

- 任務契約化的工具（用什麼產出標準格式 capsule）
- 抽驗 / 驗收親跑的可行性（無 shell 時的 fallback）
- 歷史回寫的可重現性

### 3. 已知能力盲區與 fallback

在 Gemini CLI 上做不到的事，須協作端（Engineer / 使用者）補位的部分。

### 4. 該 AI 在 PM 角色上的歷史事件

CryptoBot 專案 S70 事件中，Gemini PM 連續 ≥7 次假宣告（F1×5 + F3×3 + F5×1）觸發結構性失靈。請於本檔記錄該事件的根因分析與防範改善建議。

### 5. 模式協議實作

`output-mode-protocol.md` 在 Gemini CLI 上的具體實作（無 hook 時的 fallback 機制）。

### 6. 跨 AI 交接時的注意

當 session 由 Gemini PM 接給 Claude PM（或反向）時的注意事項。

---

## 提交方式

完成後刪除 `.placeholder` 副檔名，改名為 `gemini-cli.md`，提交 PR。

審核流程依 `GOVERNANCE.md`。
